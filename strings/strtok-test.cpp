// A C/C++ program for splitting a string using strtok() 
#include <stdio.h> 
#include <string.h> 
#include<stdlib.h> 
int main() 
{ 
//    char str[] = "Geeks for Geeks"; 
//    // Returns first token 
//    char* token = strtok(str, " "); 
//    printf("token is %s\n",token);
//    // Keep printing tokens while one of the delimiters present in str[]. 
//    while (token != NULL) { 
//        printf("%s\n", token); 
//        token = strtok(NULL, " "); 
//    } 
    //const char *str = (char *)malloc(1*1);
    const char* str ="hello how are you";
	printf("%s\n",str);  
    return 0; 
} 
